{% block content %}
{% for post in posts %}
    <div>
        <h3>{{post.title}}</h3>
        <h2>{{post.content}}</h2>
        <a href="{{post.link}}">See what I've done</a>
    </div>
    <img src="{{post.thumbnail}}">
{% endfor %}
{% endblock %}