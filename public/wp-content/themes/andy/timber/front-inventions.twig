{% block content %}
{% for post in posts %}
    <div class="background">
        <img src="{{post.thumbnail}}">
        <div class="overlay">
            <h3>{{post.title}}</h3>
            <p>{{post.content}}</p>
            <a href="{{post.link}}">[Learn more]</a>
        </div>
    </div>
{% endfor %}
{% endblock %}